PC ativo em borda de SUBIDA

pegar o ffd que usamos no banco de registradores, e mudar o falling edge pra rising edge (faz outro flip flop com o nome ffd_borda_subida)

são 32 ffps

o ffp do meio vai ter o contador diferente
aí faz generate pros da esquerda, um port map pro do meio e um generate pros da direita (nas especificações fala qual lado fica com um a mais)

o mux entre a mem instr e o banco registrador tem duas entradas, cada uma de 5 bits (faz e0 e e1, NAO faz com vetor pra nao virar matriz) 
pegar o mux que usamos na ula e fazermos um generate de 5 desse (pro mux que sai da memoria -depois da ula- faz um generate de 32)

vamo ter uqe fazer um mux de 32 tmb

funct 6 bits

extensor de sinal:
os bits de 15 a 0 da saída vão receber a entrada
faz um generate q pega o bit à esquerda e replica ele 16 vezes

deslocador:
pega os bits na posição 29 a 0 e atribui às posições 31 a 2, e insere dois zeros (down to)

atribuir um nome pra cada fio do caminho de dados
